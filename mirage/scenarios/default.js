//import { Server } from "net";

export default function(server) {
  server.createList("post", 109);
  /*
    Seed your development database using your factories.
    This data will not be loaded in your tests.
  */

  // server.createList('post', 10);
}
